[meta]
name = "Security: Secret Scan"
description = "Scan the repo for leaked secrets and credentials (chibby scan secrets)"
author = "chibby"
version = "1.0.0"
category = "security"
tags = ["security", "secrets", "gitleaks", "scan"]
required_tools = ["gitleaks"]
template_type = "stage"

[[stages]]
name = "security-secrets"
commands = ["NO_COLOR=true chibby scan secrets"]
backend = "local"
fail_fast = true
